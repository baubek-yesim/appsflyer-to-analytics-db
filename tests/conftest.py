"""Shared pytest fixtures for the appsflyer_pipeline test suite.

The project is installed editable into the uv-managed venv (`uv sync`), so
`import appsflyer_pipeline` works directly here without any import shims.
"""
